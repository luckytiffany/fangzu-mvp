# 房租水电管理 MVP · 数据安全 / 多用户隔离 设置指南

> 适用对象：非技术 Owner。照着一步步点就能完成「多用户数据互不串、互不看」的安全设置。
> 目标：10~20 个用户共用同一个 Supabase 数据库时，每个人的房源/房间/读数/设置互相隔离。

---

## 一、前置条件（应已完成）

- 已有 Supabase 项目（本项目用的是 `oegmxcrcdrzqwyeymgb`）。
- 网页前端已正确填入该项目的 URL 与 anon key（`sb_publishable_...`）。

---

## 二、运行隔离 SQL（关键步骤）

作用：开启「行级安全 RLS」并写入隔离策略，保证每个人只能看/改自己账号的数据。

> 注意：Supabase 的 SQL Editor 偶尔会因内容太长把脚本截断，因此**分两段**运行最稳妥。

### 第 1 段：建表 + 补列 + 本人隔离

1. 打开 Supabase 后台 → 左侧 **SQL Editor** → **New query**
2. 把 `supabase_secure.sql` 的「第一段」内容（也可直接复制本仓库 `supabase_secure.sql` 文件前半部分）完整粘贴
3. 粘贴后**滚动到最底部**，确认最后一行是 `select '第一段完成...'`，再点 **Run**
4. 看到提示 `第一段完成：表、列、本人隔离策略已就绪` 即成功

### 第 2 段：授权表 + 授权隔离策略

1. 再 **New query**
2. 粘贴「第二段」内容
3. 同样先滚到底确认有 `select '第二段完成...'` 这行，再点 **Run**
4. 看到 `第二段完成：授权及授权隔离策略已就绪` 即全部完成

> 幂等说明：本脚本可反复运行，不会报错或重复创建。

---

## 三、关闭 Confirm email（否则新用户登录不了）

1. Supabase 后台 → **Authentication** → **Providers**
2. 点 **Email**
3. 把 **Confirm email（确认电子邮件）** 关掉（OFF）
4. 确认 **Email sign-ups（电子邮件注册）** 是开启（ON）
5. 底部点 **Save**

> 原因：本产品用「手机号@login.local」作为登录邮箱（假邮箱），开了邮件确认会卡死新用户注册。

---

## 四、安全红线（务必记住）

| 事项 | 要求 |
|---|---|
| 前端用的 key | 只允许放 **anon / publishable** key（公开但安全，靠 RLS 隔离） |
| 绝不能放前端的 key | 任何以 `service_role` 开头的 key（万能钥匙，暴露即全库泄露） |
| 共用手机 | 别在公共手机/同一浏览器登你的账号，否则本机数据会混 |
| 授权代抄 | 是你主动把某栋楼临时交他人抄表，限定楼栋+有效期，到期自动失效，不影响其它楼栋 |

---

## 五、隔离效果自检（给 Owner 的安心清单）

- [ ] 不同用户分别注册、登录后，各自只看到自己的房源
- [ ] 无法在网页里看到别人的任何数据
- [ ] 授权给某人后，对方只能看到被授权的那栋楼
- [ ] Confirm email 已关闭，新用户注册后可直接登录

---

## 六、附：本项目相关凭据（请勿外泄 service_role）

- Supabase URL：`https://oegmxcrcdrzqwyeymgb.supabase.co`
- 前端 anon key：`sb_publishable_nUpi6djsVjIVX0PkwRj_tg_aDkAEQW3`
- GitHub 仓库：`luckytiffany/fangzu-mvp`（Pages：`https://luckytiffany.github.io/fangzu-mvp/`）
